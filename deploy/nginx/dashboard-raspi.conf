server {
  listen 8080;
  server_name _;
  # Personalizza il path in base al tuo DEPLOY_PATH reale sul Raspberry.
  root /home/deploy/workspace/dashboard-raspi/current/frontend;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}